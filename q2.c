#include <pthread.h>
#include <stdlib.h>
#include <stdio.h>
int primos = 0;

int main(int argc, char *argv[]){
    int a, b;
    scanf("%i %i", &a, &b);
    int i;
    for(i=a; i<=b; i++){
        
    }
    pthread_exit(NULL);
}